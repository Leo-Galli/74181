name: Benchmark ALU 74181

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  benchmark:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        test_range: ["0...104"]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install psutil anybadge

      - name: Build simulator
        run: gcc -o simulator 74181.c -O2 -w

      - name: Run benchmarks
        run: python .github/workflows/benchmark.py ${{ matrix.test_range }} ${{ matrix.os }}

      - name: Commit badges
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add badges/
          git commit -m "Update summary badges for ${{ matrix.os }}" || echo "No changes"
          git push
